#include <iostream>
#include <math.h>

using namespace std;


class Fraction
{
private:
    int numerator;
    int denominator;
public:
    Fraction(/* args */);
    ~Fraction();
};

Fraction::Fraction(/* args */)
{
}

Fraction::~Fraction()
{
}
